<%= simple_form_for(@cocktail) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, label: "Entrez le nom du cocktail"  %>
  <%= f.input :ingredient, collection: { Ingredient.all } %>
  <%= f.button :submit %>
<% end %>

  <%= f.button :submit %>
<% end %>
